import React from 'react';
import About from '../components/About';

const AboutPage = () => {
  // Removed inline padding styles because About.js handles it internally
  return <About />;
};

export default AboutPage;